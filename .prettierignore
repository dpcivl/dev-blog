# Ignore everything
/*

# Except these files & folders
!/src
!/public
!/.github
!tsconfig.json
!astro.config.ts
!.prettierrc.mjs
!package.json
!.prettierrc
!eslint.config.js
!README.md

# 블로그 본문은 손으로 쓴다. prettier 가 마크다운 표에 정렬 패딩을 넣는데,
# 한글은 화면에서 두 칸을 차지하지만 prettier 는 한 글자로 세기 때문에
# 오히려 어긋나 보인다. 렌더 결과는 그대로라 얻는 것도 없고, 글을 쓸 때마다
# 포맷을 돌려야 CI 가 통과하는 마찰만 생긴다.
/src/data/blog

# 외부에서 가져온 폰트 자산 — 원본 그대로 둔다 (압축된 CSS 를 펴면 커지기만 한다)
/public/fonts
